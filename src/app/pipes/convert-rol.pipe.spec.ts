import { ConvertRolPipe } from './convert-rol.pipe';

describe('ConvertRolPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRolPipe();
    expect(pipe).toBeTruthy();
  });
});
